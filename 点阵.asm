
	MOV	R5,#0
	MOV	R1,#1
L1:	LCALL	DZQP
	LCALL	XZCX
	LCALL	SYCX
	MOV	A,R1
	RL	A
	MOV	R1,A
	INC	R5
	CJNE	R5,#16,L1
	MOV	R1,#1
L2:	LCALL	DZQP		
	LCALL	XZCX
	LCALL	XYCX
	MOV	A,R1
	RL	A
	MOV	R1,A
	INC	R5
	CJNE	R5,#32,L2
	RET

SYCX:	MOV	DZ,R1
	SETB	ROW0
	CLR	ROW0
	LCALL	DZYS
	RET

XYCX:	MOV	DZ,R1
	SETB	ROW1
	CLR	ROW1
	LCALL	DZYS
	RET

DZYS:	MOV	R6,#0FFH
	DJNZ	R6,$
	RET

XZCX:	MOV	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL0
	CLR	COL0

	MOV	A,#20H
	ADD	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL2
	CLR	COL2

	INC	R5
	MOV	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL1
	CLR	COL1

	MOV	A,#20H
	ADD	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL3
	CLR	COL3
	RET
DZQP:	MOV	DZ,#0
	SETB	ROW0
	CLR	ROW0
	SETB	ROW1
	CLR	ROW1
	RET