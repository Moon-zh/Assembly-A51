
	MOV	R5,#0
	MOV	R1,#1
L1:	LCALL	DZQP
	LCALL	XZCX
	LCALL	SYCX
	MOV	A,R1
	RL	A
	MOV	R1,A
	INC	R5
	CJNE	R5,#16,L1
	MOV	R1,#1
L2:	LCALL	DZQP		
	LCALL	XZCX
	LCALL	XYCX
	MOV	A,R1
	RL	A
	MOV	R1,A
	INC	R5
	CJNE	R5,#32,L2
	RET

SYCX:	MOV	DZ,R1
	SETB	ROW0
	CLR	ROW0
	LCALL	DZYS
	RET

XYCX:	MOV	DZ,R1
	SETB	ROW1
	CLR	ROW1
	LCALL	DZYS
	RET

DZYS:	MOV	R6,#0FFH
	DJNZ	R6,$
	RET

XZCX:	MOV	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL0
	CLR	COL0

	MOV	A,#20H
	ADD	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL2
	CLR	COL2

	INC	R5
	MOV	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL1
	CLR	COL1

	MOV	A,#20H
	ADD	A,R5
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL3
	CLR	COL3
	RET
DZQP:	MOV	DZ,#0
	SETB	ROW0
	CLR	ROW0
	SETB	ROW1
	CLR	ROW1
	RET

//Êý×Ö
XZCX:	MOV	A,SW1
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL0
	CLR	COL0
	INC	SW1
	
	MOV	A,GW1
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL1
	CLR	COL1
	INC	GW1

	MOV	A,XS1
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL2		
	CLR	COL2
	INC	XS1

	MOV	A,XS2
	MOVC	A,@A+DPTR
	MOV	DZ,A
	SETB	COL3
	CLR	COL3
	INC	XS2
	RET

YSCX:	MOV	A,SW
	MOV	B,#16
	MUL	AB
	MOV	SW1,A

	MOV	A,GW
	MOV	B,#16
	MUL	AB
	MOV	GW1,A

	MOV	A,XS
	MOV	B,#16
	MUL	AB
	MOV	XS1,A

	MOV	A,XSS
	MOV	B,#16
	MUL	AB
	MOV	XS2,A
	RET